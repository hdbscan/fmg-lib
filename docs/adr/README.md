# ADRs

- `0001-ui-migration-guardrails.md`: Defines the UI migration rules for dependencies, imports, CI checks, and screenshot milestones.
- `0002-canvas-renderer-strategy.md`: Locks the pass-based Canvas2D-first renderer direction.
- `0003-ui-state-and-worker-protocol.md`: Locks the typed app-state and worker message model.

Use short ADRs for rules that must stay stable across migration steps. Update an existing ADR when clarifying the rule; add a new ADR only when the decision meaningfully changes.
